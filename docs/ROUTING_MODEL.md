# Routing Model

Each event passes through:

1. Match → which rules apply?  
2. Extract → which fields belong in this channel?  
3. Convert → which format should this channel receive?  
4. Route → which target gets which fragment?  
